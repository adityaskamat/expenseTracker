# Expense Tracker (Python)

A simple command-line expense tracker that records expenses to a CSV file and provides summary views.

## Features
- Add new expenses (date, category, description, amount)
- View all expenses
- Summarize spending by category

## Tech
- Python
- CSV file storage

## Usage
Run `python3 expense_tracker.py` in your terminal.
